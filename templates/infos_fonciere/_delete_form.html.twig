<form method="post" action="{{ path('infos_fonciere_delete', {'id': infos_fonciere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_fonciere.id) }}">
    <button class="btn">Delete</button>
</form>
