{% extends 'administration/index.html.twig' %}

{% block title %}Hello MessageController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="card">

                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover table-striped">
                            <thead class="thead-default">
                            <tr>

                                <th>Visiteur</th>
                                <th>Contact</th>
                                <th>Objet</th>
{#                                <th>categorie</th>#}

                                <th>Action</th>

                            </tr>
                            </thead>
                            {% for message in messages %}
                                <tr>

                                    <td>{{ message.visiteur.nom ~" " ~ message.visiteur.prenoms }}</td>
                                    <td>{{ message.visiteur.telephone }} <br>
                                        {{ message.visiteur.email }}</td>
                                    <td>{{ message.proprietes.libelle }}</td>
{#                                    <td>{{ message.categorie }}</td>#}
                                    {# <td> #}
                                    {# {{ bien.quartier ? bien.quartier.LibQuart: '' }}, #}
                                    {# </td>
                                    <td>{{ bien.adresse }}</td> #}


{#                                     <td><button type="button" class="btn btn-icon btn-outline-primary mr-2 mb-2"><i class="icmn-pencil" aria-hidden="true"></i></button></td>#}
{#                                     <td><button type="button" class="btn btn-icon btn-outline-danger mr-2 mb-2"><i class="icmn-cross" aria-hidden="true"></i></button></td>#}
                                    <td>

                                        <!-- Example single danger button-->
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-success dropdown-toggle"
                                                    data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                Action
                                            </button>
                                             <div class="dropdown-menu">
                                             <a class="dropdown-item" href="{{ path('admin_message_demande_renseigmenent_details',{'id':message.id}) }}"> <i class="fas fa-eye"></i> Plus de details  </a>
                                                 <a class="dropdown-item" href="#"> <i class="fas fa-trash"></i>  Supprimer</a>
                                             </div>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}

                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
