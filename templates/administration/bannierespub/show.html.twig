{% extends '../../base.html.twig' %}

{% block title %}BannierePub{% endblock %}

{% block body %}
    <h1>BannierePub</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banniere_pub.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ banniere_pub.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ banniere_pub.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ banniere_pub.createdAt ? banniere_pub.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('banniere_pub_index') }}">back to list</a>

    <a href="{{ path('banniere_pub_edit', {'id': banniere_pub.id}) }}">edit</a>

    {{ include('banniere_pub/_delete_form.html.twig') }}
{% endblock %}
