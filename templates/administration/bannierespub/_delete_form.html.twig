<form method="post" action="{{ path('admin_banniere_pub_delete', {'id': banniere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banniere.id) }}">
    <button  class="btn btn-icon btn-sm btn-danger" data-card-tool="Supprimer la bannier" data-toggle="tooltip" data-placement="top" title="Supprimer la bannier" >  <i class="ki ki-close icon-nm"></i> </button>
</form>
{#<a href="{{ path('admin_banniere_pub_delete',{'id':banniere.id}) }}" class="btn btn-icon btn-sm btn-danger" data-card-tool="Supprimer la bannier" data-toggle="tooltip" data-placement="top" title="Remove Card">#}
{#    <i class="ki ki-close icon-nm"></i>#}
{#</a>#}
